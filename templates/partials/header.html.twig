<div id="header">
    <div class="row animated fadeInDown">
        <div class="large-2 columns">
            {% if header.name %}
                <h2>{{ header.name }}</h2>
            {% endif %}   
            {% if header.description %}
                <h6 class="light">{{ header.description }}</h6>
            {% endif %}
        </div>
        <div class="large-3 columns">
            <div class="row">
                <div class="small-4 columns light2">{{ header.baseinfo_name }}</div>
                <div class="small-8 columns border-left light2 ">
                    <ul class="no-bullet">
                        {% for item in header.address %}
                            <li>{{ item.line }}</li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
        </div>
        <div class="large-3 columns">
            <div class="row">
                <div class="small-4 columns light2">{{ header.contact_name }}</div>
                <div class="small-8 columns border-left light2">
                    <ul class="no-bullet">
                        {% for item in header.contact %}
                            <li>{{ item.line }}</li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
        </div>
        <div class="large-3 columns">
            <div class="row">
                <div class="small-4 columns light2">{{ header.social_name }}</div>
                <div class="small-8 columns border-left light2">
                    <ul class="no-bullet">
                        {% for item in header.social %}
                            <li>{{ item.line }}</li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
        </div>
        <div class="large-1 columns text-right website">
            <a href="{{ header.download_url }}"><i class="fi-arrow-down"></i> {{ header.download_name }}</a>
        </div>
    </div>
</div>